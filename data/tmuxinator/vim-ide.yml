# ~/.tmuxinator/vim-ide.yml

name: vim-ide
root: ~/

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
pre_window: workon vim-ide

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

windows:
  - shell: git status
  - tmuxinator: vi ~/.tmuxinator/vim-ide.yml
  - vimrc: vi ~/.vimrc
  - editor:
      layout: 9302,195x49,0,0[195x42,0,0,1,195x6,0,43,3]
      panes:
          - vim
          - #empty (default shell)
  - presentation: cd presentations/django; vimdeck 31mar2014.md
  - server: python manage.py runserver
  - grunt_watch: grunt
  - sitepackages: cdsitepackages; ls
